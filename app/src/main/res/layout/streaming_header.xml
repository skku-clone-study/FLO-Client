<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    app:layout_constraintTop_toTopOf="parent"
    android:padding="10dp"
    >
    <ImageButton
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:id="@+id/streaming_equalizer_btn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_equalizer"
        android:background="@android:color/transparent"
        android:tint="@color/gray_100"/>
    <android.widget.Button
        app:layout_constraintStart_toEndOf="@id/streaming_equalizer_btn"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_marginStart="20sp"
        android:id="@+id/streaming_eq_btn"
        android:layout_width="40dp"
        android:layout_height="25dp"
        android:background="@drawable/corner_rounded_white_transparent"
        android:text="EQ"
        android:textColor="@color/gray_100"/>
    <ImageButton
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@android:color/transparent"
        android:src="@drawable/ic_down_arrow"
        android:tint="@color/gray_100" />
</androidx.constraintlayout.widget.ConstraintLayout>